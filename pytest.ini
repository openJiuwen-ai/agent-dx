[pytest]
# CLI source lives under agent/cli/ (the ar_cli package); tests live under agent/tests/.
# pythonpath lets `from ar_cli import ...` resolve without installing the package.
pythonpath =
    agent/cli
    agent/sdk/python/src
    agent/executor/src
testpaths = agent/tests
