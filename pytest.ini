[pytest]
# Source lives under cli/ (the ar_cli package); tests live under tests/.
# pythonpath lets `from ar_cli import ...` resolve without installing the package.
pythonpath =
    cli
    python/src
    executor/src
testpaths = tests
